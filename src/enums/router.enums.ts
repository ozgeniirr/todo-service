export enum RouterLabel {
	Otp = 'otp',
	Todo = 'todo',
	Auth = 'auth',
	User = 'user',
}

export enum RouterPath {
	RegisterEndpoint = 'register',
	SendOtp= 'send-otp',
	VerifyOtp= 'verify-otp',
	Login='login',
	Profile='profile',
	Todo='createTodo',
	UpdateTodo='update',
	DeleteTodo='delete',
	GetTodo='getTodo',
}